# EAS iOS Preview Build

iOS local production 빌드를 실행합니다.
ios, android 선택 가능

```bash
cd /Users/charles/1git/nownwoori/app && eas build --platform ios --profile production --non-interactive --local
```
